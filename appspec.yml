--- #variable Task Playbook
- hosts: ansibleGP
  user: aoudumbar
  become: yes
  connection: ssh
  tasks:
        - name: Deploy Static Web
          copy:
            src: index.html
            dest: /var/www/html
...
